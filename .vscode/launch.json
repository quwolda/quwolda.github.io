{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Запустить мой сайт",
            "file": "${workspaceFolder}/index.html" 
        }
    ]
}
